# Bridge English — Vite (web/)
# Copy to web/.env (gitignored). Never put service_role or AI keys here.

VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_or_publishable_key

# AI keys belong in supabase/functions/.env (or Dashboard Function Secrets):
#   DEEPSEEK_API_KEY=
#   AI_API_KEY=
# Do NOT prefix with VITE_ — they must never ship to the browser.
